# requirements/base.txt
# Core runtime dependencies for the app and Week 1–2 pipeline.
# Keep these pinned to protect reproducibility.

numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2

# If your Week 1–2 dashboard is Django/Flask/etc, keep those pins here as well.
# Example:
Django==5.1.15
djangorestframework==3.15.2
psycopg2-binary==2.9.9
